services:
  api:
    build: .
    ports:
      - "8080:8080"
    environment:
      PORT: "8080"
      DATABASE_URL: "data/pismo.db"
    volumes:
      - db-data:/app/data
    restart: unless-stopped

volumes:
  db-data:
